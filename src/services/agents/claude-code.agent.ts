/**
 * Backward-compatibility re-export.
 *
 * The canonical SDK agent is now in claude-sdk.agent.ts.
 * This file preserves the old import path for existing consumers.
 */
export { ClaudeSdkAgent as ClaudeCodeAgent } from './claude-sdk.agent.js';
